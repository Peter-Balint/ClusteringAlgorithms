<UserControl x:Class="Clustering.View.SetupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Clustering.View"
             xmlns:c="clr-namespace:Clustering.View.Components"
             xmlns:viewmodel="clr-namespace:Clustering.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.Resources>
            <DataTemplate DataType="{x:Type viewmodel:SetupSpatial2DViewModel}">
                <c:SetupSpatial2D />
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewmodel:SetupSpatial3DViewModel}">
                <c:SetupSpatial3D />
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewmodel:SetupImageViewModel}">
                <c:SetupImage />
            </DataTemplate>
        </Grid.Resources>
        <TextBlock
            Grid.Row="0"
            FontSize="28"
            Text="Setup"
            Margin="10"/>
        <ContentControl Grid.Row ="1" Content="{Binding DisplayViewModel}" />
    </Grid>
</UserControl>
