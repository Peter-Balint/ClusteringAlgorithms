<UserControl x:Class="Clustering.View.Components.SetupSpatial3D"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Clustering.View.Components"
             
             xmlns:h="http://helix-toolkit.org/wpf"
             xmlns:hx="http://helix-toolkit.org/wpf/SharpDX"
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <h:HelixViewport3D Grid.Row="0" Background="Black" >

            <h:HelixViewport3D.Camera>
                <PerspectiveCamera Position="10,10,10"
                       LookDirection="-10,-10,-10"
                       UpDirection="0,1,0" />
            </h:HelixViewport3D.Camera>

            <h:DefaultLights />
            
             
            <h:GridLinesVisual3D></h:GridLinesVisual3D>
            <h:SphereVisual3D Center="5,0,0" Radius="1"/>

            <ModelVisual3D>

                <h:SphereVisual3D Center="3,0,0" Radius="1" />

            </ModelVisual3D>

        </h:HelixViewport3D>

        <!--<hx:Viewport3DX Grid.Row="0">

            <hx:GroupModel3D ItemsSource="{Binding Spheres}"></hx:GroupModel3D>
            
        </hx:Viewport3DX>-->
        
        <TextBlock Grid.Row="1" Text="{Binding TestText}"/>
        <Button Grid.Row="2" Content="Add" Command="{Binding TestCommand}"/>
    </Grid>
</UserControl>
