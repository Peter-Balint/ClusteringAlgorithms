<UserControl x:Class="Clustering.View.Components.Progress"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Clustering.View.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="450">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <StackPanel>
        <TextBlock
            FontSize="28"
            Margin="10" >
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsFinished}" Value="False">
                            <Setter Property="Text" Value="Clustering in progress..."/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsFinished}" Value="True">
                            <Setter Property="Text" Value="Clustering finished"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
        <TextBlock Text="{Binding StepText}"/>
        <Button Content="Finish early on current step" Command="{Binding FinishCommand}">
            <Button.Style>
            <Style TargetType="Button">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsFinished}" Value="False">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsFinished}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
        <Button Visibility="{Binding IsFinished, Converter={StaticResource BooleanToVisibilityConverter}}"
                Content="Go to results" Command="{Binding NavigateToResultsCommand}"/>
    </StackPanel>
</UserControl>
